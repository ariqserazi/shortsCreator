# shortsCreator icons

Add production app icons here before a polished public release:

- macOS: `icon.icns`
- Windows: `icon.ico`

The current electron-builder configuration can create unsigned development installers without custom icons.
